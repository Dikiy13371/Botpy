# 🚀 Быстрый старт с Docker

## Шаг 1: Подготовка

```bash
# Скопируйте .env.example в .env
cp .env.example .env
```

## Шаг 2: Настройка .env

Откройте `.env` и заполните обязательные поля:

```env
BOT_TOKEN=ваш_токен_от_BotFather
GROUP_ID=-1003313600592
```

**Как получить BOT_TOKEN:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

**Как получить GROUP_ID:**
1. Добавьте бота в группу/канал
2. Отправьте боту команду `/getid`
3. Скопируйте ID и вставьте в `.env`

## Шаг 3: Запуск

```bash
# Запустить бота в фоне
docker-compose up -d

# Посмотреть логи
docker-compose logs -f
```

## Шаг 4: Проверка

```bash
# Проверить статус контейнера
docker ps

# Проверить логи
docker-compose logs -f

# Health check
docker exec bitrix24-bot python -c "import sys; sys.exit(0)"
```

## Управление

```bash
# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Обновить код
git pull
docker-compose build
docker-compose up -d
```

## Где находятся данные?

- **Подписчики:** `./data/subscribers.json`
- **Логи:** `./logs/bot.log`

Эти данные сохраняются между перезапусками!

## Проблемы?

См. раздел "Поддержка и Troubleshooting" в [README.md](README.md)


