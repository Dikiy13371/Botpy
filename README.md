# Telegram бот для мониторинга статуса Bitrix24

Профессиональный Telegram бот на Python для автоматического мониторинга статуса Bitrix24 и отправки уведомлений о сбоях и восстановлении сервиса.

## 🚀 Возможности

- ✅ Автоматический мониторинг статуса Bitrix24
- 🔔 Уведомления о сбоях и восстановлении в Telegram группу
- 👥 Управление подписчиками
- 📊 Статистика работы бота
- ⏱️ Отслеживание длительности сбоев
- 🌍 Поддержка разных регионов
- 🔄 Автоматическое обновление сообщений с таймером
- 📝 Подробное логирование
- 🛡️ Обработка ошибок с повторными попытками
- 🎯 Graceful shutdown

## 📋 Требования

### Для Docker (рекомендуется)
- Docker 20.10+
- Docker Compose 2.0+
- Telegram бот токен (получить у [@BotFather](https://t.me/BotFather))
- ID группы/канала для отправки алертов

### Для локального запуска
- Python 3.8+
- Telegram бот токен (получить у [@BotFather](https://t.me/BotFather))
- ID группы/канала для отправки алертов

## 🐳 Установка и запуск с Docker (рекомендуется)

### Быстрый старт

1. **Клонируйте репозиторий или скачайте файлы**

2. **Создайте файл `.env` на основе `.env.example`:**
   ```bash
   cp .env.example .env
   ```

3. **Настройте `.env` файл:**
   ```env
   BOT_TOKEN=your_bot_token_here
   GROUP_ID=-1003313600592
   CHECK_INTERVAL=300
   # ... остальные настройки
   ```

4. **Запустите бота:**
   ```bash
   docker-compose up -d
   ```

5. **Проверьте логи:**
   ```bash
   docker-compose logs -f
   ```

### Управление контейнером

```bash
# Запустить в фоне
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Проверить статус
docker ps

# Просмотр логов контейнера
docker logs bitrix24-bot

# Health check вручную
docker exec bitrix24-bot python -c "import sys; sys.exit(0)"
```

### Обновление кода

```bash
# Получить обновления
git pull

# Пересобрать образ
docker-compose build

# Перезапустить с новым образом
docker-compose up -d
```

### Структура данных

При использовании Docker данные сохраняются в локальных директориях:
- `./data/subscribers.json` - файл с подписчиками
- `./logs/bot.log` - файл логов

Эти данные сохраняются между перезапусками контейнера благодаря volumes.

## 🔧 Локальная установка (без Docker)

1. **Клонируйте репозиторий или скачайте файлы**

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте файл `.env` на основе `.env.example`:**
   ```bash
   cp .env.example .env
   ```

4. **Настройте `.env` файл:**
   ```env
   BOT_TOKEN=your_bot_token_here
   GROUP_ID=-1003313600592
   CHECK_INTERVAL=300
   # ... остальные настройки
   ```

5. **Получите ID группы/канала:**
   - Добавьте бота в группу/канал
   - Отправьте команду `/getid` боту
   - Скопируйте ID и вставьте в `.env`

## 🎮 Использование

### Запуск бота

**С Docker:**
```bash
docker-compose up -d
```

**Локально:**
```bash
python bot.py
```

### Команды бота

- `/start` - Начать работу с ботом и подписаться на уведомления
- `/help` - Показать справку
- `/subscribe` - Подписаться на уведомления
- `/unsubscribe` - Отписаться от уведомлений
- `/status` - Проверить текущий статус Bitrix24
- `/stats` - Показать статистику бота
- `/getid` - Получить ID текущего чата/группы

## 📁 Структура проекта

```
botpythonbitrix24/
├── bot.py                      # Главный файл запуска
├── Dockerfile                  # Docker образ
├── docker-compose.yml          # Docker Compose конфигурация
├── .dockerignore               # Исключения для Docker
├── config/                     # Конфигурация
│   ├── __init__.py
│   └── config.py               # Класс конфигурации
├── handlers/                   # Обработчики команд
│   ├── __init__.py
│   └── command_handlers.py     # Обработчики команд Telegram
├── services/                   # Бизнес-логика
│   ├── __init__.py
│   ├── bitrix_parser.py        # Парсер статуса Bitrix24
│   ├── subscriber_manager.py   # Управление подписчиками
│   └── status_monitor.py       # Мониторинг статуса
├── utils/                      # Утилиты
│   ├── __init__.py
│   ├── logger_config.py        # Настройка логирования
│   ├── message_formatter.py    # Форматирование сообщений
│   └── time_utils.py           # Утилиты для работы со временем
├── .env                        # Конфигурация (создать на основе .env.example)
├── .env.example               # Пример конфигурации
├── requirements.txt           # Зависимости Python
├── README.md                  # Документация
├── data/                      # Данные (создается автоматически)
│   └── subscribers.json       # Файл с подписчиками
└── logs/                      # Логи (создается автоматически)
    └── bot.log                # Файл логов
```

## ⚙️ Конфигурация

Все настройки бота находятся в файле `.env`:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | - |
| `URL` | URL страницы статуса | `https://status.bitrix24.ru/` |
| `GROUP_ID` | ID группы для алертов | - |
| `CHECK_INTERVAL` | Интервал проверки (сек) | `300` |
| `SUBSCRIBERS_FILE` | Файл с подписчиками | `data/subscribers.json` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `LOG_FILE` | Файл логов | `logs/bot.log` |
| `REQUEST_TIMEOUT` | Таймаут запросов (сек) | `10` |
| `RETRY_ATTEMPTS` | Попытки при ошибке | `3` |
| `RETRY_DELAY` | Задержка между попытками (сек) | `5` |

## 📝 Логирование

Бот ведет подробные логи в файл `logs/bot.log` (или указанный в `LOG_FILE`) и выводит их в консоль. Логи ротируются при достижении 10 МБ (сохраняется 5 резервных копий).

**При использовании Docker:** логи доступны в `./logs/bot.log` на хосте.

Уровни логирования:
- `DEBUG` - Подробная отладочная информация
- `INFO` - Общая информация о работе
- `WARNING` - Предупреждения
- `ERROR` - Ошибки
- `CRITICAL` - Критические ошибки

## 🔒 Безопасность

⚠️ **Важно:**
- Никогда не публикуйте файл `.env` с реальными токенами
- Добавьте `.env` в `.gitignore`
- Храните токены в безопасном месте
- Не делитесь токеном бота публично

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
# Установите зависимости для разработки
pip install -r requirements.txt

# Установите LOG_LEVEL=DEBUG в .env для подробных логов
LOG_LEVEL=DEBUG

# Запустите бота
python bot.py
```

### Тестирование

Для тестирования бота:
1. Создайте тестовую группу в Telegram
2. Добавьте бота в группу
3. Используйте команду `/getid` для получения ID группы
4. Установите `GROUP_ID` в `.env`
5. Запустите бота и проверьте работу команд

## 🐛 Обработка ошибок

Бот включает в себя:
- Автоматические повторные попытки при сетевых ошибках
- Graceful shutdown при получении сигналов SIGINT/SIGTERM
- Валидацию конфигурации при запуске
- Обработку исключений во всех критических местах
- Подробное логирование ошибок

## 📊 Метрики

Бот отслеживает:
- Время работы (uptime)
- Количество отправленных алертов
- Количество восстановлений
- Количество проверок статуса
- Количество подписчиков

## 🤝 Вклад

Если вы хотите улучшить бота:
1. Создайте форк проекта
2. Внесите изменения
3. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется свободно. Используйте на свой страх и риск.

## ⚠️ Отказ от ответственности

Этот бот создан для информационных целей. Автор не несет ответственности за любые проблемы, возникшие в результате использования этого бота.

## 📞 Поддержка и Troubleshooting

### Проверка работы

**С Docker:**
```bash
# Проверить статус контейнера
docker ps

# Просмотр логов
docker-compose logs -f

# Проверить health check
docker exec bitrix24-bot python -c "import sys; sys.exit(0)"
```

**Локально:**
- Проверьте логи в файле `logs/bot.log`
- Убедитесь, что процесс Python запущен

### Решение проблем

1. **Бот не запускается:**
   - Проверьте логи: `docker-compose logs` или `logs/bot.log`
   - Убедитесь, что все настройки в `.env` корректны
   - Проверьте, что `BOT_TOKEN` и `GROUP_ID` установлены

2. **Бот не отправляет сообщения:**
   - Убедитесь, что бот добавлен в группу
   - Проверьте, что бот имеет права на отправку сообщений
   - Проверьте `GROUP_ID` командой `/getid`

3. **Ошибки при парсинге:**
   - Проверьте интернет-соединение
   - Убедитесь, что URL `https://status.bitrix24.ru/` доступен
   - Проверьте логи на наличие ошибок запросов

4. **Данные не сохраняются (Docker):**
   - Убедитесь, что volumes настроены в `docker-compose.yml`
   - Проверьте права доступа к директориям `./data` и `./logs`
   - Убедитесь, что контейнер не пересоздается без volumes

5. **Контейнер падает:**
   - Проверьте логи: `docker-compose logs`
   - Убедитесь, что все переменные окружения установлены
   - Проверьте health check: `docker exec bitrix24-bot python -c "import sys; sys.exit(0)"`

---

**Создано с ❤️ для мониторинга Bitrix24**

