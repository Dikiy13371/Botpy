# Инструкция по миграции на новую версию

## ⚠️ Важно: Изменения в структуре проекта

Код был полностью рефакторен для production-ready использования. Старый код сохранен в `bot.py`, но теперь используется новая модульная структура.

## 🔄 Шаги миграции

### 1. Сохраните текущие настройки

Из старого `bot.py` скопируйте следующие значения:
- `BOT_TOKEN` (строка 13)
- `GROUP_ID` (строка 17)

### 2. Создайте файл `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
BOT_TOKEN=ваш_токен_здесь
GROUP_ID=-1003313600592
URL=https://status.bitrix24.ru/
CHECK_INTERVAL=300
SUBSCRIBERS_FILE=subscribers.json
LOG_LEVEL=INFO
LOG_FILE=bot.log
REQUEST_TIMEOUT=10
RETRY_ATTEMPTS=3
RETRY_DELAY=5
```

**Важно:** Замените `ваш_токен_здесь` на реальный токен из старого кода.

### 3. Установите новые зависимости

```bash
pip install -r requirements.txt
```

Новые зависимости:
- `python-dotenv` - для работы с .env файлами

### 4. Запустите бота

```bash
python bot.py
```

## 📁 Новая структура проекта

```
botpythonbitrix24/
├── bot.py                      # Главный файл (обновлен)
├── config/                     # Конфигурация
│   └── config.py
├── handlers/                   # Обработчики команд
│   └── command_handlers.py
├── services/                   # Бизнес-логика
│   ├── bitrix_parser.py
│   ├── subscriber_manager.py
│   └── status_monitor.py
├── utils/                      # Утилиты
│   ├── logger_config.py
│   ├── message_formatter.py
│   └── time_utils.py
├── .env                        # Ваши настройки (создать!)
├── .env.example               # Пример настроек
├── requirements.txt           # Зависимости
└── README.md                  # Документация
```

## ✨ Новые возможности

1. **Логирование в файл** - все логи сохраняются в `bot.log`
2. **Конфигурация через .env** - все настройки в одном месте
3. **Обработка ошибок** - автоматические повторные попытки
4. **Graceful shutdown** - корректное завершение работы
5. **Валидация конфигурации** - проверка настроек при запуске
6. **Метрики** - отслеживание работы бота

## 🔍 Проверка работы

После запуска проверьте:

1. Бот отвечает на команды `/start`, `/help`
2. В группе появляются уведомления о статусе
3. Создается файл `bot.log` с логами
4. Файл `subscribers.json` создается автоматически

## ❓ Проблемы?

Если что-то не работает:

1. Проверьте файл `.env` - все ли настройки заполнены
2. Проверьте логи в `bot.log`
3. Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
4. Проверьте, что бот добавлен в группу и имеет права на отправку сообщений

## 📝 Примечания

- Старый код в `bot.py` был полностью заменен
- Все функции сохранены и работают так же
- Подписчики из старого `subscribers.json` будут автоматически загружены
- Логи теперь пишутся в файл, а не только в консоль

