# Changelog - Масштабный рефакторинг

## Версия 2.0.0 - Рефакторинг и улучшения

### 🔄 Переход на базу данных SQLite

- **Заменен JSON на SQLite** с использованием `aiosqlite`
- Создан класс `Database` для управления БД
- Таблицы:
  - `subscribers` - подписчики бота
  - `incidents` - история всех инцидентов
- Все операции с БД полностью асинхронны

### 🎯 Инлайн-кнопки

- Добавлена функция `create_alert_buttons()` для алертов
- Кнопки в алертах:
  - "🔄 Проверить статус сейчас"
  - "📊 История инцидентов"
- Callback обработчики для всех кнопок

### 📊 Расширенный мониторинг компонентов

- Парсер теперь определяет затронутые компоненты:
  - CRM
  - Почта
  - Задачи
  - Диск
  - Календарь
  - Телефония
- Компоненты отображаются в алертах
- Компоненты сохраняются в истории инцидентов

### ⚡ Улучшение асинхронности

- Все операции с сетью переведены на `aiohttp`
- Все операции с БД асинхронны через `aiosqlite`
- `StatusMonitor` полностью асинхронный
- Убраны все блокирующие вызовы

### 🔄 Exponential Backoff

- Реализован экспоненциальный backoff для retry
- Формула: `base_delay * (2 ^ (attempt - 1))`
- Добавлен jitter для избежания thundering herd
- Специальная обработка 502/503 ошибок

### 📋 Команда /history

- Показывает последние 5 завершенных инцидентов
- Отображает:
  - Время начала и конца
  - Длительность сбоя
  - Затронутые компоненты
  - Регион

### 🛠️ Технические улучшения

- Обновлен `requirements.txt`:
  - `aiohttp>=3.9.0` (вместо requests)
  - `aiosqlite>=0.19.0` (для БД)
- Обновлен `Dockerfile`:
  - Убрана переменная `SUBSCRIBERS_FILE` (теперь БД)
- Улучшена обработка ошибок
- Улучшено логирование

### 📝 Новые файлы

- `services/database.py` - управление БД
- `CHANGELOG.md` - этот файл

### 🔧 Измененные файлы

- `services/subscriber_manager.py` - полностью переписан на async + SQLite
- `services/incident_tracker.py` - полностью переписан на async + SQLite
- `services/bitrix_parser.py` - async + компоненты + exponential backoff
- `services/status_monitor.py` - полностью async
- `handlers/command_handlers.py` - async версии всех команд
- `utils/message_formatter.py` - добавлены инлайн-кнопки
- `bot.py` - инициализация БД и async компонентов

### ⚠️ Миграция данных

При первом запуске:
- Старые JSON файлы (`subscribers.json`, `incidents.json`) не используются
- Данные нужно будет добавить заново или мигрировать вручную
- База данных создается автоматически в `data/bot.db`

### 🚀 Запуск

```bash
# Обновить зависимости
pip install -r requirements.txt

# Или с Docker
docker-compose build
docker-compose up -d
```

### 📊 Новые команды

- `/history` - последние 5 инцидентов с подробностями

### 🔄 Улучшенные команды

- `/incidents` - теперь показывает компоненты
- `/export` - теперь включает компоненты в CSV


