# 📋 Руководство по ручному тестированию бота

## Перед началом

1. **Установи зависимости:**
   ```bash
   # Linux/WSL
   python3 -m pip install -r requirements.txt
   
   # Windows
   pip install -r requirements.txt
   ```

2. Создай тестовую группу/личный чат в Telegram
3. Добавь туда бота
4. Убедись что в .env установлен GROUP_ID этого чата
5. Включи логирование: `LOG_LEVEL=DEBUG` в .env

## 🧪 Сценарии тестирования

### Сценарий 1: Базовые команды (5 минут)

```
1. Отправь /start
   ✅ Должна появиться приветственное сообщение
   ✅ Должно быть главное меню с кнопками
   
2. Отправь /help
   ✅ Должна появиться справка со списком команд
   
3. Отправь /status
   ✅ Должен появиться текущий статус Bitrix24
   ✅ Должны быть кнопки для быстрых действий
   
4. Отправь /stats
   ✅ Должна быть статистика (подписчики, интервал)
   
5. Отправь /metrics
   ✅ Должны быть подробные метрики (uptime, алерты и т.д)
```

### Сценарий 2: Меню и кнопки (10 минут)

```
1. Отправь /menu
   ✅ Появится главное меню
   
2. Нажми "📊 Мониторинг & Статус"
   ✅ Откроется подменю мониторинга
   
3. Нажми "🔄 Проверить статус"
   ✅ Покажет статус
   ✅ Будут кнопки "Статус", "Метрики", "История", "Меню"
   
4. Нажми "🏠 Меню"
   ✅ Вернулся в главное меню
   
5. Нажми "🔔 Управление подписками"
   ✅ Покажет меню подписок
   
6. Нажми "✅ Подписаться" (если не подписан)
   ✅ Сообщение: "Вы успешно подписались"
   ✅ Кнопка изменится на "❌ Отписаться"
   
7. Нажми "❌ Отписаться"
   ✅ Сообщение: "Вы отписались"
   ✅ Кнопка вернулась на "✅ Подписаться"
```

### Сценарий 3: История и инциденты (5 минут)

```
1. Отправь /incidents
   ✅ Покажет активные инциденты (если есть)
   ✅ Покажет недавние закрытые инциденты
   
2. Отправь /history
   ✅ Покажет последние 5 инцидентов
   
3. Отправь /export
   ✅ Отправит CSV файл с инцидентами
   ✅ Файл можно открыть в Excel
```

### Сценарий 4: Здоровье и логи (5 минут)

```
1. Отправь /health
   ✅ Покажет статус здоровья бота:
      - Telegram API доступен
      - Bitrix24 доступен
      - БД в порядке
   ✅ Покажет количество ошибок за час
   
2. Отправь /logs
   ✅ Покажет последние 15 строк логов
   ✅ Должны быть логи вроде INFO, DEBUG, ERROR
```

### Сценарий 5: Изменение статуса (симуляция)

```
1. Отправь команду пока Bitrix24 работает (/status)
   ✅ Должна быть ✅ "ВСЕ ОТЛИЧНО РАБОТАЕТ"
   
2. Если есть реальный сбой - просто жди алерта
   ✅ Должен прийти алерт через CHECK_INTERVAL секунд
   
3. Если нет сбоя - создай вручную через БД:
   ```bash
   docker exec bitrix24-bot sqlite3 /app/data/bot.db \
   "INSERT INTO incidents (region, components, description, status, start_time) \
   VALUES ('ru', 'CRM,Mail', 'Test incident', 'active', datetime('now'));"
   ```
   ✅ Должна появиться запись в истории
```

### Сценарий 6: Параллельные действия (5 минут)

```
1. В одной группе нажимай кнопки быстро (5-10 кликов подряд)
   ✅ Бот должен ответить на каждый клик
   ✅ Никаких ошибок в консоли
   
2. В разных чатах одновременно отправляй команды:
   - Чат 1: /status
   - Чат 2: /metrics
   - Чат 3: /incidents
   ✅ Все должны ответить почти одновременно
   ✅ Никаких конфликтов в БД
```

### Сценарий 7: Граничные случаи (10 минут)

```
1. Отправь очень длинную команду
   ✅ Бот не должен зависнуть
   ✅ Либо ошибка, либо проигнорирует
   
2. Отправь спецсимволы: /status !@#$%^&*()
   ✅ Бот не должен упасть
   
3. Добавь бота в 3-4 разные группы
   ✅ Каждой группе должен отправляться правильный GROUP_ID
   ✅ Никаких сообщений в неправильные группы
   
4. Отправь /status когда нет интернета
   ✅ Бот должен вернуть ошибку (не зависнуть)
   ✅ В логах exponential backoff
   
5. Отправь команду когда Telegram API недоступен
   ✅ Бот должен логировать ошибку
   ✅ Следующая команда должна работать
```

### Сценарий 8: Администраторские команды (5 минут)

```
Примечание: эти команды работают только если твой user_id = ADMIN_CHAT_ID

1. Отправь /menu → ⚙️ Администрирование
   ✅ Откроется админ-меню (если ты админ)
   
2. Нажми "🧪 Проверить подключения"
   ✅ Покажет статус всех сервисов (✅ или ❌)
   
3. Нажми "🔧 Информация о БД"
   ✅ Покажет:
      - Количество инцидентов
      - Количество подписчиков
      - Размер БД
   
4. Нажми "📋 Последние логи"
   ✅ Покажет последние 15 строк логов
```

## 🐛 Дебажинг проблем

### Проблема: Бот не отвечает на команды

```bash
# 1. Проверь логи
docker-compose logs -f

# 2. Проверь что бот запущен
docker ps | grep bitrix24

# 3. Проверь конфиг
cat .env | grep BOT_TOKEN GROUP_ID

# 4. Перезагрузи бота
docker-compose restart
```

### Проблема: Кнопки не работают

```bash
# Проверь логи callback обработчика
docker-compose logs -f | grep "callback"

# Убедись что callback_data совпадает с именами в button_callback
grep "callback_data=" handlers/command_handlers.py
```

### Проблема: БД ломается

```bash
# Проверь целостность БД
docker exec bitrix24-bot sqlite3 /app/data/bot.db "PRAGMA integrity_check;"

# Если поломана - удали и пересоздай
docker exec bitrix24-bot rm /app/data/bot.db
docker-compose restart
```

### Проблема: Много ошибок в логах

```bash
# Включи DEBUG логирование
sed -i 's/LOG_LEVEL=.*/LOG_LEVEL=DEBUG/' .env
docker-compose restart

# Просмотри логи с фильтром
docker-compose logs | grep ERROR
docker-compose logs | grep WARNING
```

## ✅ Чек-лист перед продакшеном

- [ ] Все команды работают (/start, /status, /menu и т.д)
- [ ] Все кнопки работают (меню, подменю, быстрые кнопки)
- [ ] Нет ошибок в логах (ERROR, CRITICAL)
- [ ] БД консистентна (PRAGMA integrity_check OK)
- [ ] Бот отвечает на несколько команд одновременно
- [ ] Graceful shutdown работает (Ctrl+C)
- [ ] При перезагрузке восстанавливаются активные инциденты
- [ ] Логирование полное и информативное
- [ ] Нет memory leaks (занимаемая память стабильна)

